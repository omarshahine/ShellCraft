name: ShellCraft
options:
  bundleIdPrefix: com.shahine
  deploymentTarget:
    macOS: "26.0"
  xcodeVersion: "16.0"
  minimumXcodeGenVersion: "2.40"
  groupSortPosition: top
  createIntermediateGroups: true
  defaultConfig: Debug
packages:
  Sparkle:
    url: https://github.com/sparkle-project/Sparkle
    from: "2.8.1"
settings:
  base:
    SWIFT_VERSION: "6.0"
    MACOSX_DEPLOYMENT_TARGET: "26.0"
    ENABLE_HARDENED_RUNTIME: true
    SWIFT_STRICT_CONCURRENCY: complete
targets:
  ShellCraft:
    type: application
    platform: macOS
    sources:
      - path: ShellCraft
        excludes:
          - "**/*.xcassets/AppIcon.appiconset/Contents.json"
          - "ShellCraft.icon"
      - path: ShellCraft/ShellCraft.icon
        type: folder
        buildPhase: resources
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.shahine.ShellCraft
        PRODUCT_NAME: ShellCraft
        INFOPLIST_GENERATION_MODE: GeneratedByXcode
        CODE_SIGN_ENTITLEMENTS: ShellCraft/ShellCraft.entitlements
        CODE_SIGN_STYLE: Automatic
        DEVELOPMENT_TEAM: N9DRSTM2U6
        MARKETING_VERSION: "1.1"
        CURRENT_PROJECT_VERSION: 2
        GENERATE_INFOPLIST_FILE: true
        INFOPLIST_KEY_LSApplicationCategoryType: "public.app-category.developer-tools"
        ASSETCATALOG_COMPILER_APPICON_NAME: ShellCraft
        INFOPLIST_KEY_CFBundleDisplayName: ShellCraft
        # Sparkle's keys live in ShellCraft/Info.plist, which Xcode merges its
        # generated keys on top of. They cannot be INFOPLIST_KEY_ settings:
        # Xcode only honours those for keys it recognises and drops the rest
        # without warning.
        INFOPLIST_FILE: ShellCraft/Info.plist
    dependencies:
      - package: Sparkle
    entitlements:
      path: ShellCraft/ShellCraft.entitlements
  ShellCraftTests:
    type: bundle.unit-test
    platform: macOS
    sources:
      - path: ShellCraftTests
    dependencies:
      - target: ShellCraft
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.shahine.ShellCraftTests
        GENERATE_INFOPLIST_FILE: true
